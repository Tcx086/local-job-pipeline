# Role Profile Examples

These files are fake public examples that demonstrate the shape of role-specific profile YAML. They are not real resumes and should not be used for actual applications.

For private use, create your own files in `local_resources/role_profiles/`. Only `*.example.yaml` files should be committed under `resources/role_profiles/`.