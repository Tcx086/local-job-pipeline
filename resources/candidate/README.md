# Candidate Resources

This folder contains public-safe example files only. Do not place real resume facts in tracked files here.

To use the project locally, copy `resources/candidate/master_profile.example.yaml` to `local_resources/candidate/master_profile.yaml`, then replace every placeholder with your own private local information. The `local_resources/` directory is ignored by Git.